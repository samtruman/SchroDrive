{
  "name": "schrodrive",
  "version": "0.11.5",
  "private": true,
  "description": "Unified cloud debrid mount manager — integrates Overseerr, Prowlarr/Jackett, RealDebrid, TorBox, Plex, Jellyfin, and Emby",
  "main": "dist/index.js",
  "bin": {
    "schrodrive": "dist/index.js"
  },
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "bun dist/index.js serve",
    "serve": "bun dist/index.js serve",
    "dev": "bun --watch src/index.ts serve",
    "typecheck": "tsc --noEmit",
    "prepare": "git rev-parse --git-dir > /dev/null 2>&1 && git config core.hooksPath .githooks || true",
    "test": "bun test tests/unit tests/e2e tests/regressions",
    "test:unit": "bun test tests/unit",
    "test:e2e": "bun test tests/e2e",
    "test:regressions": "bun test tests/regressions",
    "test:integration": "bun tests/integration.test.ts",
    "impact": "bun scripts/impact.ts",
    "impact:changed": "bun scripts/impact.ts --changed",
    "fix:history": "bash scripts/fix-stale-history.sh"
  },
  "engines": {
    "bun": ">=1.2.0"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "busboy": "1.6.0",
    "commander": "^14.0.2",
    "cors": "^2.8.5",
    "dropbox": "^10.34.0",
    "express": "^5.1.0",
    "googleapis": "^173.0.0",
    "megajs": "^1.3.10",
    "node-torbox-api": "^0.1.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/busboy": "1.5.4",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.5",
    "@types/node": "^24.10.0",
    "typescript": "^5.9.3"
  }
}
